import {
    attribute,
    autoGeneratedHashKey,
    table,
    versionAttribute
} from '@aws/dynamodb-data-mapper-annotations';

import { Models } from 'utilities-techsweave';

// interface SpecValue {
//     fieldName: string;
//     unitMisure: string;
//     value: any;
// }

/*
 * This class rappresent a product item in products table
 */
@table(process.env.PRODUCTS_TABLE)
class Product implements Models.Tables.Product {
    @autoGeneratedHashKey()
    id: string;

    @versionAttribute()
    version: Date;

    @attribute()
    title: string;

    @attribute()
    description?: string;

    @attribute()
    price?: number;

    @attribute()
    discount?: number;

    @attribute()
    availabilityQta?: number;

    @attribute()
    imageURL?: string;

    @attribute()
    isSalable?: boolean;

    @attribute()
    categorieId?: string;

    @attribute()
    notes?: string;

    @attribute()
    tags?: string[];

    @attribute()
    customSpecs?: Models.Tables.SpecValue[];

}

export default Product;